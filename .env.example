# Repo-root secrets for dev tooling only (not the backend — that's backend/.env).
# Copy to .env in the repo root.

# Free key for /images: https://unsplash.com/developers -> New Application -> Access Key
UNSPLASH_ACCESS_KEY=
